{
  "name": "web-application",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "scope": "turbo run dev --scope=web",
    "start": "pnpm run start --filter services",
    "heroku-postbuild": "npx turbo run build --scope=services --includeDependencies --no-deps"
  },
  "devDependencies": {
    "prettier": "^2.6.2",
    "turbo": "^1.2.8"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=14.0.0 <17"
  },
  "packageManager": "pnpm@6.31.0"
}
